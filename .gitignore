.DS_Store
__pycache__/
*.pyc
venv/

# runtime/data artifacts - not source, regenerated by the connectors
downloads/
extracted/
parsed/
*.db
*.duckdb

# dbt - logs can capture secrets (e.g. resolved connection strings), regenerated on run
layers/gold_dbt/logs/
layers/gold_dbt/target/
layers/gold_dbt/dbt_packages/
*.log

# not ready to push yet (python unit tests only - leading slash anchors this
# to the repo root, so it doesn't also match dbt's own tracked tests/ under
# layers/gold_dbt/)
/tests/
notebooks/
.claude/

# dash - .env has the Azure secrets, never commit it
dash/.env
